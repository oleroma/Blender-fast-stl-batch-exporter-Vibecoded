schema_version = "1.0.0"
id = "fast_batch_stl_exporter"
version = "3.0.0"
name = "Fast Batch STL Exporter with flags"
tagline = "Export to stl of collections to a directory with sub folders"
description = "High-performance export of collections to a root directory with sub folders using viewport evaluated data. Override geometry nodes inputs on export."
maintainer = "AI"
type = "add-on"
build_date = "2026-09-02"
license = [
    "SPDX:GPL-3.0-or-later"
]
tags = ["Import-Export"]
blender_version_min = "5.2.0"
